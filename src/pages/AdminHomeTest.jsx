import React from 'react';
import { Box, Typography } from '@mui/material';

function AdminHomeTest() {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Admin Panel Test</Typography>
            <Typography variant="body1">If you can see this, the routing works!</Typography>
        </Box>
    );
}

export default AdminHomeTest;
